
//A simple function that says hello to us but is modular and can be reused anywhere for any service as required
export default function sayHello(input:string) :string {
    return `Hello ${input}`;
}

